/**
 * Canonical publish-destination registry — SSOT for app-shell + Next + legacy Vite.
 *
 * Implementation lives in `./publish-destinations/`. This file keeps the
 * package export path `@workspace/app-shell/publish-destinations` stable.
 */

export * from "./publish-destinations/index";
