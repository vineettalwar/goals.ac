# @workspace/api-spec

OpenAPI sources + bundle. After path edits: `pnpm --filter @workspace/api-spec run codegen`.

**Consumers:** workspace packages via pnpm.

Monorepo map: [../../docs/CODEBASE.md](../../docs/CODEBASE.md).
