# @workspace/ai-providers

Gemini / Bedrock / Ollama abstraction with tier routing (`strategy` / `planning` / `execution` / `rapid`).

**Consumers:** workspace packages via pnpm.

Monorepo map: [../../docs/CODEBASE.md](../../docs/CODEBASE.md).
