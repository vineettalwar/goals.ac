# @workspace/goals-ac

Legacy Vite redirect shell (port 5173). Not production.

**Frozen:** do not add product features here. Production UI is `artifacts/goals-app-ui` + `lib/app-shell`.

**Runtime:** legacy / opt-in local only.

Monorepo map: [../../docs/CODEBASE.md](../../docs/CODEBASE.md).
