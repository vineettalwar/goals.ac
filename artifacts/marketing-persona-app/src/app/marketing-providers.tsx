"use client";

/** Minimal client shell for static marketing export — no auth, query, or toast runtime. */
export function RootProviders({ children }: { children: React.ReactNode }) {
  return children;
}
